using hashmap
store first value as char and second as integer
//num = 24


HashMap<Character, Integer> map = new HashMap<>();
map.put("I", 1);
map.put("V",5);
map.put("X",10);
map.put("L",50);
map.put("C",100);
map.put("D",500);
map.put("M",1000);

int rem=0;
int ans=0;
int val = 0;
for(char i = s.toCharArray()){ 
    val = map.get(i);
    if(val > num){
        rem = num%val;
    }
 
}

return val;
